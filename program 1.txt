program 1
git --version
mkdir 4AD24CI005
cd 4AD24CI005
git init 
git status
gedit f1.txt
git status
gedit f2.txt
git status
git add f1.txt
git status
gedit f3.txt
git add
git commit-m"new commit"
git log

program 2
mkdir git_conflict_demo2
cd  git_conflict_demo2
git init
gedit file.txt
echo "line 1">file.txt
gedit file.txt
echo "line 2">file.txt
gedit file.txt
git add file.txt
git status
git commit-m"initial commit"
git status
git branch 
git checkout -b branch-a
git branch
echo"change from branch a">file.txt
git commit-m "edit from branch a"
gedit file.txt
git checkout master
git branch
git checkout -b branch-b
git branch
echo"change from branch b">file.txt
git commit-m "edit from branch b"
git checkout branch -a
git merge branch -b
gedit file.txt
git add file.txt
git commit-m "resolved conflict b/w branch a"

program 3
mkdir stashex
cd stashex
git init
echo "statsh example">file.txt
git add .
git stash
o/p
git commit -m"initial commit"
git stash
o/p
git checkout branch_a
git branch branch_a
git checkout branch_a
echo "some contents to stash"
git add .
git stash
o/p
git checkout branch_b_name
git branch branch_b_name
git checkout branch_b_name
echo "some text to add">bfile.txt
git add .
git  commit -m "fix the bug in branch B"
git checkout branch_a
git stash pop


program 4 
mkdir reponew
cd reponew
git init
ls
git clone"url"
ls
cd repotest
ls
echo'first push'>f1.txt
ls
git add .
git commit -m "initial commit"
git status
git remote set -url origin "url"
git push origin

program 5
mkdir newrebaserepo
cd newrebaserepo
git init
ls
cd rebaserepo
git log
git checkout -b feature
git log
git branch
echo "f1 added">f1.txt
git add .
git commit -m"f1 added"
echo "f2 added">f2.txt
git add .
git commit -m"f2 added"
git log
git checkout main
git log
git rebase feature
git log
git push origin main
git remote set -url origin"url"
git push origin main
git push origin feature



